TZ=Asia/Dhaka
AUTH_BASE_URL=
# JSON array of allowed origins for CORS (for testing when API doesn't return allowed_origins)
ALLOWED_ORIGINS=["http://localhost:4000","http://localhost:3000"]
# Set to false to allow all origins (use '*' for CORS)
IS_CHECK_ALLOWED_ORIGINS=true
